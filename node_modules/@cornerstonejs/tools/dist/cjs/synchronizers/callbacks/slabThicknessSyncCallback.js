"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@cornerstonejs/core");
function slabThicknessSyncCallback(_synchronizerInstance, sourceViewport, targetViewport) {
    var _a, _b, _c, _d;
    const renderingEngine = (0, core_1.getRenderingEngine)(targetViewport.renderingEngineId);
    if (!renderingEngine) {
        throw new Error(`No RenderingEngine for Id: ${targetViewport.renderingEngineId}`);
    }
    const tViewport = renderingEngine.getViewport(targetViewport.viewportId);
    const sViewport = renderingEngine.getViewport(sourceViewport.viewportId);
    const slabThickness = (_b = (_a = sViewport).getSlabThickness) === null || _b === void 0 ? void 0 : _b.call(_a);
    if (!slabThickness) {
        return;
    }
    (_d = (_c = tViewport).setSlabThickness) === null || _d === void 0 ? void 0 : _d.call(_c, slabThickness);
    tViewport.render();
}
exports.default = slabThicknessSyncCallback;
//# sourceMappingURL=slabThicknessSyncCallback.js.map