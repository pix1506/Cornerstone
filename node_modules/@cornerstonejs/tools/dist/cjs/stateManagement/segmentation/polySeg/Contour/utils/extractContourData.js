"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.extractContourData = void 0;
function extractContourData(polyDataCache, segmentIndexMap) {
    const rawResults = new Map();
    for (const [cacheId, intersectionInfo] of polyDataCache) {
        const surfaceId = cacheId.split('_')[1];
        for (const [_, result] of intersectionInfo) {
            if (!result) {
                continue;
            }
            const segmentIndex = Number(surfaceId) || (segmentIndexMap === null || segmentIndexMap === void 0 ? void 0 : segmentIndexMap.get(surfaceId));
            if (!segmentIndex) {
                continue;
            }
            if (!rawResults.has(segmentIndex)) {
                rawResults.set(segmentIndex, []);
            }
            rawResults.get(segmentIndex).push(result);
        }
    }
    return rawResults;
}
exports.extractContourData = extractContourData;
//# sourceMappingURL=extractContourData.js.map